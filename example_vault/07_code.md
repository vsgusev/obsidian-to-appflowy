# Code

## With language

```python
def greet(name: str) -> str:
    return f"Hello, {name}!"

result = greet("world")
print(result)
```

```javascript
const add = (a, b) => a + b;
console.log(add(1, 2));
```

```sql
SELECT name, age
FROM users
WHERE age > 30
ORDER BY name;
```

## Without language

```
plain text code block
no syntax highlighting
just monospace
```

## Inline code in text

Use `print()` to output. The `user_id` variable holds the value.
